# For the training 
1. You will place a loader screen so that when the neural network is training, it shows a loader screen loading and turning, and once the training session is over, the loader disappers, and it automatically displays the inference plots of the validation accuracy, accuracy, loss, and validation loss. 

2. The when the users go back to the dashboard page, there will be a select tag that users will be able to select the model they trained on before perfoming predictions. 